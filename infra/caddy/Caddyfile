{$CADDY_SITE_ADDRESS} {
    encode gzip zstd

    log {
        output stdout
        format json
    }

    @api path /api /api/* /healthz /readyz
    handle @api {
        reverse_proxy api:8000
    }

    handle {
        reverse_proxy web:3000
    }
}
